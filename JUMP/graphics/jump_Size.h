#pragma once

//======================================================================================================================
namespace jump
{
    //==================================================================================================================
    /** Represents the width and height of a GUI element. */
    template <typename ValueType>
    struct Size
    {
        ValueType width;
        ValueType height;
    };
}   // namespace jump
